class CheckFieldEntity{
  final String result;
  final String? playerId;
  final String? playerName;
  final String? message;

  CheckFieldEntity(this.result, this.playerId, this.playerName, this.message);
}